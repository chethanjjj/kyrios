[build-system]
requires = ["setuptools >= 42.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "kyrios"
description = "ml package to process healthcare data to detect at-risk patients"
version = "0.1"
readme = "README.md"
