[build-system]
requires = ["setuptools >= 69.0"]
build-backend = "setuptools.build_meta"

[project]
name = "kyrios"
description = "ml package to process and detect AI-generated audio data"
version = 0.1
readme = "README.md"
